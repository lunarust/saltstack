file_roots:
  base:
    - /srv/saltstack
  salt:
    - /srv/saltstack/config/
  zabbix:
    - /srv/saltstack/config/
  grafana:
    - /srv/saltstack/config/
  

pillar_roots:
  base:
    - /srv/saltstack/pillars
